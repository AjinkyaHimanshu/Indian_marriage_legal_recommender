# Copy to `.env` and fill in. Loaded automatically via python-dotenv.
# These take precedence over config.yaml.

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional — only needed if Hugging Face model downloads are gated for you.
HUGGINGFACE_API_KEY=
